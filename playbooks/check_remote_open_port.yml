- hosts: "all"
  vars_files:
   - ../group_vars/all.yml
   - ../group_vars/secrets.yml
  gather_facts: no
  tasks:
  - name: check remote open port
    include_role:
      name: common
      tasks_from: check_remote_open_port
    #vars:
    #  remote_server: "{{ remote_server }}"
    #  remote_port: "{{ remote_port }}"